$('users_list').update("<%= escape_javascript(render(:partial => 'users_list'))%>");
history.replaceState({page: "<%= params[:page] ? params[:page] : '1' %>"<% if params[:utf8]
        %>, utf8: "<%= params[:utf8] %>"<% end %><% if @q
        %>, q: "<%= @q %>"<% end%><% if params[:commit]
        %>, commit: "<%= params[:commit] %>"<% end%>},
  'Users', "<%= users_path %><%= 
  p = []
  p << %{page=} + params[:page] if params[:page]
  p << %{utf8=} + params[:utf8] if params[:utf8]
  p << %{q=} + @q if @q
  p << %{comit=} + params[:commit] if params[:commit]
  %{?} + p.join(%{&}) unless p.empty?
%>");